<% if @perform_merge %>
    console.log('Merge it up!');
<% else %>
    var html = "<%= escape_javascript(render partial: 'modals/merge_confirmation', locals: { feedback_1: @feedback_1, feedback_2: @feedback_2 } ) %>";
    $(html).prependTo('body');
<% end %>