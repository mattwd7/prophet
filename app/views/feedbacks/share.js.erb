closeModal();
var html = "<%= escape_javascript(render partial: 'feedbacks/log', locals: { log: @log } ) %>";
var feedback_id = <%= @feedback.id %>;
$('#feedback-' + feedback_id).find(".comments").append(html);
$('#feedback-' + feedback_id).find('.peers .number').text(<%= @feedback.peers.count %>);