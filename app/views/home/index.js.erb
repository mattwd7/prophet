var pagination = $('.pagination');
pagination.eq(0).closest('#infinite-scrolling').remove();
$('#feedbacks').append('<%= j render partial: 'feedbacks/index', locals: { feedbacks: @feedbacks } %>');
<% if @feedbacks.next_page %>
    identifyFreshFeedbacks();
<% end %>